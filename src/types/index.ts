export interface rootReducer {
  checkLogin: boolean;
}
export interface EmployeesProp {
  name: string;
  birth: number;
  gender: string;
  address: string;
  phone: string;
  createdAt?: string;
  updatedAt?: string;
}
